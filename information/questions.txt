2.1) как в БД выглядит отношение один-ко-многим? правильно я понимаю, что в задании используется именно отношение
один-ко-многим? а в примере userTech было многие-ко-многим?

2) нужно ли использовать каскадное удаление? триггеры?
как я понимаю, правильно все-таки настроить каскадное удаление пользователя -> удаление всех счетов, связанных с ним.
таким образом не придется реализовывать их в коде, и эти операции будут выполняться быстрее в MySQL
но тут встает вопрос - если вдруг в данный момент кто-то меняет значение счета, а мы в этот момент его удаляем,
если  удаление будет реализовано не в коде, то база просто не узнает, что нельзя сейчас удалять, и таки удалит,
и это приведет к ошибкам
Я решил сделать каскадное удаление. правильно я понимаю, что фактически у меня нечего изменять, кроме id?

3) как реализовать нагрузочное тестирование? касательно: Тесты должны симулировать ситуацию, когда более ста
пользователей банковских счетов одновременно активно используют возможности системы, и проверять
корректность конечного состояния банковских счетов

мое решение: тест будет создавать более 100 потоков, которые одновременно будут делать N количество операций,
Проврека, что все работает корректно: изначально задать некоторую сумму денег, например, 10.000 ед. и равномерно
рспределить ее по юзерам, затем запустить тест, и проверкой будет являться контрольная сумма всех счетов - она должна быть
опять 10.000 ед

4) что это такое? : Запрещается использовать Optimistic/Pessimistic Locking (и другие техники) средствами СУБД

4.) как правильно организовать подключение к базе? ведь подключение, отключение занимает какое-то время. каждый раз
создавать новое соединение в котроллере? как правильно организовать единственное подключение на время работы с базой?
 когда его закрывать?

5) что сюда должно входить? : Необходимо составить тест­план для нагрузочного тестирования системы

6) как реализовать потоко безопасность?
понятно, что нужно синхронизировать методы изменения состояния счета, а нужно ли синхронизировать чтение?
т.е. пример - одновременно кто-то просматирвает состояние счета А, и в этот момент, кто-то другой меняет это состояние.
или ничего страшного - просто чтение будет отображать состояние счета именно на момент просмотра, и ничего страшного, что
это состояние может измениться через секунду?

1)что такое Jmeter? касательно: Для создания тестов возможно использовать JMeter

- есть ли какая то важность в использовании именно Tomcat7, а не 8?

Разговор со Стасом 16/12/15

-использовать синглтон
-выделить интрефейс для менеджера
- почитать сервлетный контекст (надо ли?)
- задать вопросы Лоялти
  1)можно ли обойтись одной таблицой? может ли пользователь иметь несколько счетов?
  2)нужно делать информационную таблицу - проведенные транзакции
  3)относится ли Optimistic/Pessimistic Locking (и другие техники) к каскадному удалению/изменению таблиц?
- одна страница - один сервлет
- bootstrap для оформления http://getbootstrap.com/css/#tables
- синглтон - ленивый или нет?
- в hibernate как правильно сделать реализацию многопоточности - создать одно соединение или же делать одно соединение на каждый поток?
- читать/использовать Jmeter